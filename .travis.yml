# Resolve hostnames
addons:
  hosts:
    - db

# Setup python
language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt

# Setup database
services:
- postgresql

# Migrate database and run tests
script:
    - ./scripts/migrate.sh
    - ./scripts/test.sh
